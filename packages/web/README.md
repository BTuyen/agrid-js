# Agrid Web

> 🌾 **Agrid Web SDK** – A lightweight event tracking and feature flag library for web analytics, customized for the Agrid ecosystem.

This package is published to npm as [`agrid-js`](https://www.npmjs.com/package/agrid-js)
and serves as the official web tracking SDK for the **Agrid Platform**.

It is a **reduced feature set** package compared to full analytics suites — designed for modern SPAs, Chrome extensions, and embedded dashboards.
If your goal is lightweight analytics and feature flagging, this is the ideal choice.

---

## 🚀 Installation

```bash
npm i -s agrid-js
# or
yarn add agrid-js
# or
pnpm add agrid-js
```
